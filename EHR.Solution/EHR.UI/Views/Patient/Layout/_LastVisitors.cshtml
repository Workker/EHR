@using EHR.UI.Models
<div style="padding-top: 30px">
    <h4>Últimos Visitantes</h4>
    <hr />
    <ul>
       @* @foreach (ViewModel view in ViewBag.LastVisitors)
        {
            <li style="border-bottom: 1px solid #e9e9e9; ">
                <span>@(view.Account.FirstName + " " + view.Account.LastName)</span><br />
                <span>CRM: @(view.Account.ProfessionalRegistration.FirstOrDefault().Number)</span>
            </li>
        }*@
    </ul>
</div>
